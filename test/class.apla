class A {
    var x: int;

    func square(x: int) int { 
        return x * x;
    }
}

var a = A();
#a.x = 3;
#std(print(a.x));
#a.x = a.x * a.x;
#std(print(a.x));
std(print(a.square(3)));
